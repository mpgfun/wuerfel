{
  "scripts": {
    "build": "mkdir -p dist && npx esbuild app.ts --bundle --outfile=dist/bundle.js && cp *.html *.css *.png dist/"
  },
  "dependencies": {
    "typescript": "^5.9.3",
    "esbuild": "^0.19.0"
  }
}
